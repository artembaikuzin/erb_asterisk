<% q = 'operators' %>
<%= render 'queues', name: q, members: 15, offset: 500 %>
<%= render 'queues', name: 'test', members: 3, offset: 1000 %>

<%= render 'empty_args' %>

<%= apply_line_to :all_queues, q %>

<%= render 'vars_check', context: 'true-context', send_pai: true %>
